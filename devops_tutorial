git config --global user.name "githubanil2021"
git config --global user.email "akumar.contact@gmail.com"
git config --list
cd /d --go to d drive
git branch -m main --change master to main
git remote add origin https://github.com/githubanil2021/devopsrepo.git
git push -u origin main
git restore index --before staging changes
git restore --staged app.js //in stage area
git revert abcd1 // for committed reverse
git reset --soft HEAD~1 //for reset but keep in staging area
git reset --mixed HEAD~1 //for reset but keep in working area
git reset --hard HEAD~1 //for reset but remove from working area
git branch second //create new branch
